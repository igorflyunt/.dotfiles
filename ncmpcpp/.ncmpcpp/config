mpd_host = localhost

mpd_port = 6600

mpd_music_dir = ~/Music

visualizer_fifo_path = /tmp/mpd.fifo

visualizer_output_name = "my fifo"

visualizer_in_stereo = yes

visualizer_sample_multiplier = 2

visualizer_sync_interval = 10

visualizer_type = "wave" (spectrum/wave)
#visualizer_type = "spectrum" (spectrum/wave)

visualizer_look =••
#visualizer_look = --

playlist_disable_highlight_delay = 1

song_list_format = {$8%l$9 $8$9 $5%a$9 $8$9 $3%30t$9}{$R}{$(22)%50b$9}

song_status_format = {$5%a$9 $8$9 $3%t$9 $8$9 $(22)%b$9}

song_library_format = {%n - }{%t}|{%f}


now_playing_prefix ={$(13)$9 }

song_columns_list_format = (30)[32]{a} (35)[248]{t|f:Title} (35)[248]{b} (7f)[40]{l}

playlist_separate_albums = no

playlist_display_mode = classic

autocenter_mode = yes

centered_cursor = yes 

progressbar_look = "── "
#progressbar_look = "  "

progressbar_boldness = no

user_interface = classic 

header_visibility = yes

statusbar_visibility = yes

titles_visibility = no

follow_now_playing_lyrics = yes

fetch_lyrics_for_current_song_in_background = yes

screen_switcher_mode = playlist, media_library

startup_screen = media_library

locked_screen_width_part = 70

clock_display_seconds = yes

display_bitrate = yes

colors_enabled = yes

header_window_color = blue

volume_color = blue

state_line_color = 1

state_flags_color = white

main_window_color = white

color1 = blue

main_window_highlight_color = 250

progressbar_color = 235

progressbar_elapsed_color = blue

statusbar_color = white

alternative_ui_separator_color = 235

active_column_color = 240

window_border_color = 1
